Optional C++ OpenCV CLI (Sobel / median only). Build with CMake and set `OpenCV_DIR` to your OpenCV build directory.

```bash
cmake -B build -DOpenCV_DIR=<path>
cmake --build build --config Release
```
